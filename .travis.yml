sudo: required
services:
    - docker

before_install:
    - docker build -t sykeben/terraform-image .

# Terraform Plan, Apply, and Configure jobs.
jobs:
    include:
        - stage: plan
          script:
            - docker run -v $(pwd):/infra --env AZUSER=$AZ_USERNAME --env AZPASS=$AZ_PASSWORD sykeben/terraform-image terraform plan -out planfile
        - stage: apply
          script:
            - docker run -v $(pwd):/infra --env AZUSER=$AZ_USERNAME --env AZPASS=$AZ_PASSWORD sykeben/terraform-plan terrafrom apply "planfile"
        - stage: configure
          script:
            - echo "Running configure stage on docker container..."

stages:
    - plan
    - apply
    - configure

branches:
    only:
        - master